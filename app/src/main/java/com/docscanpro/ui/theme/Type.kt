package com.docscanpro.ui.theme

import androidx.compose.material3.Typography

// The web app uses the Figtree font family; on Android we fall back to the
// platform default sans-serif and rely on Material3's default type scale
// (FontFamily.Default) until a bundled Figtree font is added.
val Typography = Typography()
